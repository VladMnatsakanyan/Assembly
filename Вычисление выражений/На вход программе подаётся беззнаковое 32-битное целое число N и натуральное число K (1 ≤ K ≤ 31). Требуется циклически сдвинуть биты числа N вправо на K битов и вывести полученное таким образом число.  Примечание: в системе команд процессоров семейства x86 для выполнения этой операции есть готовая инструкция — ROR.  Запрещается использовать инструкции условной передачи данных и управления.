%include "io.inc"
section .bss
N resd 1
K resb 1
section .text
global main
main:

    GET_UDEC 4, N
    GET_DEC 2, K
    mov eax, dword[N]
    mov cl, byte[K]
    ror eax, cl
    PRINT_UDEC 4, eax
    xor eax, eax 
    ret
