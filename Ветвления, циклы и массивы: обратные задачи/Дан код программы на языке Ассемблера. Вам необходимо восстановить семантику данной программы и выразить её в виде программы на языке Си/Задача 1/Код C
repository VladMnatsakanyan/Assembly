#include <stdio.h>

int main(void) {
    int n;
    unsigned int a = 0;
    unsigned int b = 1;

    scanf("%d", &n);

    while (n--) {
        a = a ^ b;
        b = b ^ a;
        a = a ^ b;
        b = b + a;
    }

    printf("%u\n", a);

    return 0;
}
